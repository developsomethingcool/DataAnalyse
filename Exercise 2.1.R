#Exercise 2.1
print(5 * (659 - 34))

#Exercise 2.2
a <- 103
b <- 14

outcome <- round(a / b, digits = 3)

#Exercise 2.3
a <- 103 #assign a value 103
b <- 14 #assign a value 14

outcome <- round(a / b, digits = ) #divide a and b and round the answer to three digits

#Res
signif(x, digits = 2) # rounds the values in first argument to 2 significant digits